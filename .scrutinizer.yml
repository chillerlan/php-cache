build:
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run
  environment:
    php: 8.1.16

filter:
  excluded_paths:
    - config/*
    - tests/*
    - vendor/*
    - .github/*
    - .phan/*
